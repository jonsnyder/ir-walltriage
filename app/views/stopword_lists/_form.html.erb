<%= form_for(@stopword_list) do |f| %>
  <% if @stopword_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stopword_list.errors.count, "error") %> prohibited this stopword_list from being saved:</h2>

      <ul>
      <% @stopword_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
